# Feature-to-screen alignment — in progress

Baseline: `3366b4e8ed79f8203dc283952b1aec828f207f7e`, rechecked against remote main. Working branch: `codex/classcare-stabilize`. This is source evidence plus explicitly identified local tests, not verification of Netlify or deployed Firestore rules.

Working = tested stated behavior; partial = implemented with gaps; absent = no matching implementation established; unverified = runtime not checked. A passing permission test does not establish complete screen usability.

| Capability and screen | Source/data evidence | Status and acceptance check | Design implication / conflict |
|---|---|---|---|
| Staff access, all staff screens | `firestore.rules` isTeacher/isAdmin; `users.role`, `pending_approval`, `disabled`; `teacher/scanner.js` auth callback | WORKING in 11 emulator rule tests: approved teachers across classes, disabled/pending denied, no self-approval. Full account onboarding UNVERIFIED. | User confirmed ALL classes for approved staff. Assignment is context, not an authorization boundary. |
| Student score privacy | Rules for `grades`, `summativeScores`; `student/index.html`, `student/grades.js`, `js/holistic-portals.js` studentView | WORKING in rules tests: student score reads denied. Browser regression PASS. | Adviser says scores are staff-only. Student schedule remains; score table/report-card UI removed. |
| Daily QR attendance | `teacher/scanner.html`, `teacher/scanner.js` handleDecodedText; `js/kiosk-data.js` saveAttendance; daily `attendance` document | PARTIAL: transaction-based arrival/time-out/duplicate handling repaired; actual daily arrival and duplicate browser checks PASS. Time-out edge cases remain to test. Camera NOT RUN. | Actual manual controls are `manual-qr-input` and `btn-manual-scan`, not the old test's manual-id controls. Show confirmed saves only. |
| Daily optional check-in | `teacher/scanner.js` KIOSK_3STEP_QUESTIONS, finish3StepEmotionalCheck; `attendance.emotion_checkin_3step`, `emotional_checkins` | PARTIAL: mood/stress/need; skip preserves attendance. Response and attendance update now atomic, retry shown on failure. Three-step save browser check PASS; skip and failed-save retry still need targeted browser checks. | Prototype's five legacy questions cannot replace these three. Missing answers must not become positive defaults. |
| Four-mood fallback | `js/kiosk-data.js` fastMood, saveAttendance; Happy/Okay/Sad/Stressed | PARTIAL: fallback for scanner layouts without the three-step overlay; explicit skip added. Not the normal updated scanner screen. | Do not portray this fallback as the only daily workflow. |
| Deep check | `teacher/deep-check.html`, `teacher/kiosk.js`, `js/holistic-core.js` QUESTIONS, `emotional_checkins.answers` | PARTIAL: five explicit mood/stress/motivation/support/needs answers; staff-only writes and malformed-record denial pass emulator. Review before Save restored. Browser PASS for explicit answers and offline retry. | Separate optional activity. It does not record attendance. Gesture input remains unverified on actual equipment. |
| Term grades | `teacher/grades.js`; `grades.term1`–`term4`; current 60–100 input range, 30/50/20 component weighting | UNVERIFIED end-to-end. Validate saved values, missing terms, concurrent changes and export. | Older prototype uses 0–100. Do not claim this code's “DepEd standard” wording establishes an approved school grading policy. |
| Summative assessments/scores | `teacher/summative.html/js`; `summativeAssessments`, `summativeScores`; score 0–maxScore, configurable threshold | WORKING in rule validation tests; five-row batches save 15 students. Browser PASS for explicit answers and offline retry. Publish now atomically saves assessment + in-app schedule records. | Separate assessment entry from term grades. Empty is not zero. Threshold remains the teacher-entered value. |
| Shared correlation | `js/holistic-core.js` correlate; `js/holistic-portals.js` evaluate; `careAlerts` | PARTIAL: 8 domain tests pass. Latest score per subject within 30 days, below its threshold, AND negative check-in within 14 days. Auto-resolves when evidence no longer qualifies. | This is not the adviser's full attendance + declining-score + emotional-trend analysis. Do not show fabricated trend percentages or claim validated prediction. |
| Competing correlation | `teacher/summative.js` runHolisticCorrelationCheck | PARTIAL / CONFLICT: another writer uses low score AND negative emotion OR two recorded absences; unordered limited queries; no equivalent history window. Shares alert IDs with the first engine. | Product/technical checkpoint: choose one rule with team. A second writer can overwrite/reopen/resolve the same alert inconsistently. Integration of analytic claims is blocked. |
| Parent/guardian notifications | `js/telegram-alert.js`, `config/firebase-config.js`, scanner dispatch; email/Telegram, pending alerts | UNVERIFIED. Disabled in local test configuration; external delivery not exercised. | Distinguish queued, failed, unavailable and actually delivered. In-app schedule save is not parent notification delivery. |
| Reports | `teacher/export.js`; CSV/XLSX from attendance | PARTIAL: q1–q5 headings repaired to match legacy questions; separate current mood/stress/request fields. Downloaded workbook/CSV QA pending. | Do not label attendance export as the complete five-question deep-check report. Missing attendance = Not recorded. |
| Offline persistence | `js/offline-sync.js`, IndexedDB ClassCareOfflineSync v4; `sw.js` | WORKING in 5 queue tests: arrival/time-out merge, simultaneous enqueue, retries, account isolation, update-during-flush. Browser restart/reconnection NOT RUN. | New kiosk transaction requires connection; existing queued paths synchronize in an authenticated foreground tab. No background-delivery promise. Old unowned queue entries retained but not replayed as another user. |
| Other existing screens | Enrollment, assignments, helpdesk, concerns, wheel, admin tools | UNVERIFIED in this pass. | Preserve existing routes; do not imply four prototype views cover the whole application. |

## Integration checkpoint

NOT COMPLETE. Remaining gates: browser failures resolved; analytics conflict decided; current questionnaire/score policy reconciled; every included control mapped to a permitted operation and acceptance check. No frontend integration branch has been created. Current production data and services remain untouched.
