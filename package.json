{
  "name": "classcare",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:rules": "firebase emulators:exec --only firestore --project demo-classcare \"node --test tests/firestore.rules.cjs\"",
    "test:browser": "node tests/browser.cjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0",
    "playwright": "^1.55.0"
  }
}
